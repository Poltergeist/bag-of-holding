{
  "name": "@bag-of-holding/api",
  "version": "1.0.0",
  "private": true,
  "description": "Placeholder API for future sync functionality",
  "scripts": {
    "dev": "echo 'API placeholder - no dev server yet'",
    "build": "echo 'API placeholder - no build yet'",
    "typecheck": "tsc -b",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}